  <nav class="navbar navbar-inverse navbar-fixed-top"
     role="navigation"
     id="navbar-flash">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button"
                    class="navbar-toggle"
                    data-toggle="collapse"
                    data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "Earmarked", root_path, class: "navbar-brand" %>
      </div>

      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><%= link_to "Candidates", candidates_path %><span class="sr-only">(current)</span></a></li>
          <li><%= link_to "Issues", issues_path %><span class="sr-only">(current)</span></a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right" id="navbar">
          <li>
            <%= link_to root_path do %>
              <span class="glyphicon glyphicon-home"></span>
            <% end %>
          </li>
          <li id="cart">
            <%= link_to cart_path, class: "cart" do %>
              <span class="glyphicon glyphicon-shopping-cart">
              </span> Cart<%= ": $" + cart.total.to_s if cart.total %>
            <% end %>
          </li>
            <% if current_user %>
              <li>
                <a href="/profile">
                  <%= "Welcome, #{current_user.username}." %>
                </a>
              </li>
              <li>
                <%= link_to "Logout", logout_path, method: :delete %>
              </li>
                <% else %>
              <li>
                <%= link_to "Join", join_path %>
              </li>
              <li>
                <%= link_to "Login", login_path %>
              </li>
            <% end %>
        </ul>
      </div>
    </div>
</nav>