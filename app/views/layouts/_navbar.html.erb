  <nav class="navbar navbar-inverse navbar-fixed-top"
     role="navigation"
     id="navbar-flash">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button"
                    class="navbar-toggle"
                    data-toggle="collapse"
                    data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "Earforked", root_path, class: "navbar-brand", id: "logo" %>
      </div>

      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><%= link_to "Candidates", candidates_path, id: "hovered" %><span class="sr-only">(current)</span></li>
          <li><%= link_to "Issues", issues_path, id: "hovered" %><span class="sr-only">(current)</span></li>
        </ul>
        <ul class="nav navbar-nav navbar-right" id="navbar">
          <li id="cart">
            <%= link_to cart_path, class: "cart", id: "hovered" do %>
              <span class="glyphicon glyphicon-shopping-cart"></span>
              <%= "Cart: $" + cart.total.to_s if cart.total %>
            <% end %>
          </li>
          <% if platform_admin? %>
            <%= render "layouts/platform_admin_navbar" %>
          <% elsif campaign_manager? %>
            <%= render "layouts/campaign_manager_navbar" %>
          <% elsif registered_user? %>
            <%= render "layouts/registered_user_navbar" %>
          <% else %>
            <%= render "layouts/guest_navbar" %>
          <% end %>
        </ul>
      </div>
    </div>
</nav>
