<!-- Navigation -->
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation" id="navbar-flash">
  <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
          </button>
          <ul class="nav navbar-nav navbar-left" id="navbar">
            <li id="logo">
              <%= link_to "Earmarked", root_path, class: "navbar-brand" %>
            </li>
            <li>
              <%= link_to "Candidates", candidates_path %>
            </li>
            <li>
              <%= link_to "Issues", issues_path %>
            </li>
          </ul>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav navbar-right" id="navbar">
            <li>
              <%= link_to root_path do %>
              <span class="glyphicon glyphicon-home"></span>
              <% end %>
            </li>
            <li id="cart">
              <%= link_to cart_path do %>
              <span class="glyphicon glyphicon-shopping-cart"></span><%= ": $" + cart.total.to_s if cart.total %>
              <% end %>
            </li>
              <% if current_user %>
            <li>
              <a href="/profile"><%= "Welcome, #{current_user.username.capitalize}." %></a>
            </li>
            <li>
              <%= link_to "Logout", logout_path, method: :delete %>
            </li>
              <% else %>
            <li>
              <%= link_to "Join", join_path %>
            </li>
            <li>
              <%= link_to "Login", login_path %>
            </li>
              <% end %>
          </ul>
      </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container -->
</nav>
